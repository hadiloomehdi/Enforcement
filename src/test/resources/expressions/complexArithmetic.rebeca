reactiveclass RebecaClass(5) {

	knownrebecs {
	}

	statevars {
		int op1;
		int op2;
		boolean result;
	}

	RebecaClass() {
	    op1 = 25;
	    op2 = 5;
	    result = ((op1 + op2) - op1) * (op1 * op1) * (op1 / op2) * (op2 - op1)  + (23 / 4);

	}

}

main {
	RebecaClass obj ():();
}